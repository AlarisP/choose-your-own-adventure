## AI is not allowed to edit this file
## Only the repository owner, the human, is allowed to edit this file

*** 4/13/26 ***
7:42 PM
Here is my role for the project, follow it and produce the necessary code:

Person 3 — Project Shell + GitHub Pages
Files: index.html, shared style.css, nav component, gh-pages config

* Build the landing/home page (index.html) with links to Reader and Graph
* Write shared CSS (colors, fonts, layout) that Person 1 and 2 import
* Set up the GitHub repo + enable GitHub Pages
* Write a simple nav snippet (just HTML) both pages include
* Integration: once Person 1 and 2 are done, wire up "View in graph" / "Read from here" cross-links between the two pages
Person 3 can deliver shared CSS and the home page in day 1, then the integration work comes last.


No Conflicts
Each person owns separate files. The only shared thing is style.css (Person 3 owns it; 1 and 2 just link to it). No one is editing the same file.